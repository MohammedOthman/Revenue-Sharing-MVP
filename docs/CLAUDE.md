# CLAUDE.md — Revenue-Sharing-MVP

Handoff so a future session can continue without rediscovering the tree.

## What this repo is

- **`app/`** — the product. TanStack Start action runtime. Start at `app/src/lib/reven/`.
- **`docs/RUNTIME.md`** — governing contract for the **software**. Read it before any strategy paper.
- **`docs/`** — venture strategy, pricing, GTM, market papers. Capture → Settle → Orchestrate there is the **commercial** phasing, not the software cut.
- **`brand/`** — marks.
- **`README.md`** — repository guide.

There is no `revenue-share-platform/`. That Express + React + JWT app was removed on 2026-09-12.

## Conventions

- Eleven frozen verbs. Connectors submit evidence. Humans decide credit and record payouts.
- Journals append. Claim rows are projections.
- Integer minor units. SAR default.
- `user_id` on every row.
- Do not add a payment rail. Recording a payout is a milestone, not a transfer.
