# Copy to .env only if you host the frontend separately from the backend API.
# Point it at the backend's /api path. Leave unset for the single-service deploy
# (where Express serves this frontend and same-origin /api works).
# VITE_API_URL=https://api.example.com/api
